@page "/tipoturno/editar/{id:int}"
@using CentroSenderos_2026_Shared.DTO
@inject IHttpServicio Http
@inject NavigationManager Nav
@rendermode InteractiveAuto

<div class="turno-form-page">

    <div class="turno-card">

        <div class="card-header">

            <div>
                <h1>Editar Tipo de Turno</h1>
                <p>Modifique los datos del tipo de turno.</p>
            </div>

            <button type="button"
                    class="close-button"
                    title="Cerrar"
                    @onclick="Cancelar">
                <i class="bi bi-x-lg"></i>
            </button>

        </div>


        @if (cargando)
        {
            <div class="loading-state">

                <div class="spinner-border"
                     role="status">
                    <span class="visually-hidden">Cargando...</span>
                </div>

                <span>Cargando tipo de turno...</span>

            </div>
        }
        else
        {
            <EditForm Model="@modelo"
                      OnValidSubmit="Grabar"
                      class="turno-form">

                <DataAnnotationsValidator />

                <ValidationSummary class="validation-summary" />


                <div class="field-group">

                    <label>TIPO DE TURNO</label>

                    <InputText class="senderos-input"
                               @bind-Value="modelo.Tipo"
                               placeholder="Ej: Sesión individual, Evaluación inicial..." />

                    <ValidationMessage For="@(() => modelo.Tipo)" />

                </div>


                <div class="field-group">

                    <label>DESCRIPCIÓN</label>

                    <InputTextArea class="senderos-textarea"
                                   @bind-Value="modelo.Descripcion"
                                   placeholder="Ingrese una descripción del tipo de turno" />

                    <ValidationMessage For="@(() => modelo.Descripcion)" />

                </div>


                <div class="field-group">

                    <label>DURACIÓN</label>

                    <div class="duration-input-group">

                        <div class="duration-icon">
                            <i class="bi bi-clock"></i>
                        </div>

                        <InputNumber class="duration-input"
                                     @bind-Value="modelo.DuracionMinutos"
                                     min="0"
                                     step="5" />

                        <div class="duration-suffix">
                            minutos
                        </div>

                    </div>

                    <ValidationMessage For="@(() => modelo.DuracionMinutos)" />

                </div>


                @if (!string.IsNullOrWhiteSpace(Mensaje))
                {
                    <div class="form-message">
                        <i class="bi bi-exclamation-circle"></i>
                        <span>@Mensaje</span>
                    </div>
                }


                <div class="form-actions editar-actions">

                    <button type="button"
                            class="btn-eliminar"
                            @onclick="AbrirConfirmacionEliminar">

                        <i class="bi bi-trash3"></i>
                        Eliminar Tipo de Turno

                    </button>


                    <div class="right-actions">

                        <button type="button"
                                class="btn-cancelar"
                                @onclick="Cancelar">
                            Cancelar
                        </button>

                        <button type="submit"
                                class="btn-guardar"
                                disabled="@guardando">

                            @if (guardando)
                            {
                                <span class="spinner-border spinner-border-sm"></span>
                                <span>Guardando...</span>
                            }
                            else
                            {
                                <span>Guardar Cambios</span>
                            }

                        </button>

                    </div>

                </div>

            </EditForm>
        }

    </div>

</div>


@if (mostrarConfirmacionEliminar)
{
    <div class="modal-overlay">

        <div class="delete-modal">

            <div class="delete-warning-icon">
                <i class="bi bi-exclamation-triangle-fill"></i>
            </div>

            <h2>
                ¿Estás seguro que querés eliminar este tipo de turno?
            </h2>

            <p>
                Esta acción no se puede deshacer.
                El tipo de turno será eliminado definitivamente.
            </p>

            <div class="delete-modal-actions">

                <button type="button"
                        class="confirm-delete-button"
                        disabled="@eliminando"
                        @onclick="ConfirmarEliminar">

                    @if (eliminando)
                    {
                        <span class="spinner-border spinner-border-sm"></span>
                        <span>Eliminando...</span>
                    }
                    else
                    {
                        <span>Eliminar</span>
                    }

                </button>

                <button type="button"
                        class="modal-cancel-button"
                        disabled="@eliminando"
                        @onclick="CerrarConfirmacionEliminar">
                    Cancelar
                </button>

            </div>

        </div>

    </div>
}


@code {

    [Parameter]
    public int id { get; set; }


    private TipoTurnoDTO modelo = CrearModeloVacio();

    private bool cargando = true;

    private bool guardando = false;

    private bool eliminando = false;

    private bool mostrarConfirmacionEliminar = false;

    private string Mensaje = string.Empty;


    protected override async Task OnInitializedAsync()
    {
        await CargarTipoTurno();
    }


    private async Task CargarTipoTurno()
    {
        cargando = true;
        Mensaje = string.Empty;

        var resp =
            await Http.Get<TipoTurnoDTO>(
                $"api/tipoturno/{id}"
            );

        if (resp.Error)
        {
            modelo = CrearModeloVacio();
            Mensaje = resp.ObtenerError();
        }
        else
        {
            modelo =
                resp.Respuesta
                ?? CrearModeloVacio();
        }

        cargando = false;
    }


    private async Task Grabar()
    {
        guardando = true;
        Mensaje = string.Empty;

        var resp =
            await Http.Put<TipoTurnoDTO, string>(
                $"api/tipoturno/{id}",
                modelo
            );

        guardando = false;

        if (!resp.Error)
        {
            Nav.NavigateTo(
                "/tipoturno",
                true
            );
        }
        else
        {
            Mensaje = resp.ObtenerError();
        }
    }


    private void AbrirConfirmacionEliminar()
    {
        mostrarConfirmacionEliminar = true;
    }


    private void CerrarConfirmacionEliminar()
    {
        if (!eliminando)
        {
            mostrarConfirmacionEliminar = false;
        }
    }


    private async Task ConfirmarEliminar()
    {
        eliminando = true;
        Mensaje = string.Empty;

        var resp =
            await Http.Delete(
                $"api/tipoturno/{id}"
            );

        eliminando = false;

        if (!resp.Error)
        {
            mostrarConfirmacionEliminar = false;

            Nav.NavigateTo(
                "/tipoturno",
                true
            );
        }
        else
        {
            Mensaje = resp.ObtenerError();

            mostrarConfirmacionEliminar = false;
        }
    }


    private void Cancelar()
    {
        Nav.NavigateTo("/tipoturno");
    }


    private static TipoTurnoDTO CrearModeloVacio()
    {
        return new TipoTurnoDTO
        {
            Tipo = string.Empty,
            Descripcion = string.Empty,
            DuracionMinutos = 0
        };
    }

}