@implements IDisposable
@inject NavigationManager NavigationManager


<div class="sidebar">

    <!-- Header -->
    <div class="sidebar-header">
            <img src="imagenes/logo-senderos.png" alt="Logo Senderos" />

@* <div class="top-row ps-3 navbar navbar-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="turnos">Centro Integral Senderos</a>
    </div>
</div> *@
    </div>

@* <input type="checkbox" title="Navigation menu" class="navbar-toggler" /> *@

<div class="nav-scrollable" onclick="document.querySelector('.navbar-toggler').click()">
    <nav class="nav flex-column">

        @*      <div class="nav-item px-3">
                <NavLink class="nav-link" href="" Match="NavLinkMatch.All">
                    <span class="bi bi-house-door-fill-nav-menu" aria-hidden="true"></span> Home
                </NavLink>
            </div> *@

        @* <div class="nav-item px-3">
            <NavLink class="nav-link" href="turnos">
                <span class="bi bi-plus-square-fill-nav-menu" aria-hidden="true"></span> Turnos
            </NavLink>
        </div> *@

    </nav>
</div>

    <!-- Menú -->
    <nav class="sidebar-nav">

@*         <NavLink href="Account/Login" Match="NavLinkMatch.All" class="sidebar-link">
            <i class="bi bi-house-door"></i>
            <span>Inicio</span>
        </NavLink> *@

        <NavLink href="auth" class="sidebar-link">
            <i class="bi bi-lock"></i>
            <span>Auth</span>
        </NavLink> *@

        <AuthorizeView>

            <Authorized>

                <NavLink href="turnos" class="sidebar-link">
                    <i class="bi bi-calendar"></i>
                    <span>Turnos</span>
                </NavLink>

                <NavLink href="profesionales" class="sidebar-link">
                    <i class="bi bi-person-workspace"></i>
                    <span>Profesionales</span>
                </NavLink>

                <NavLink href="pacientes" class="sidebar-link">
                    <i class="bi bi-people"></i>
                    <span>Pacientes</span>
                </NavLink>

                <NavLink href="admin" class="sidebar-link">
                    <i class="bi bi-shield-lock"></i>
                    <span>Administración</span>
                </NavLink>

              @*   <a href="/swagger"
                   target="_blank"
                   class="sidebar-link">

                    <i class="bi bi-journal-code"></i>

                    <span>Swagger</span>

                </a> *@

            </Authorized>

        </AuthorizeView>

    </nav>

    <!-- Footer -->

    <div class="sidebar-footer">

        <AuthorizeView>

            <Authorized>

                <NavLink href="Account/Manage"
                         class="sidebar-link">

                    <i class="bi bi-person-circle"></i>

                    @* <span>@context.User.Identity?.Name</span> *@

                </NavLink>

                <form action="Account/Logout" method="post">

                    <AntiforgeryToken />

                    <input type="hidden"
                           name="ReturnUrl"
                           value="@currentUrl" />

                    <button type="submit"
                            class="sidebar-link logout-button">

                        <i class="bi bi-box-arrow-left"></i>

                        @* <span>Cerrar sesión</span> *@

                    </button>

                </form>

            </Authorized>

            <NotAuthorized>
@* 
                <NavLink href="Account/Login"
                         class="sidebar-link">

                    <i class="bi bi-box-arrow-in-right"></i>

                    <span>Ingresar</span>

                </NavLink> *@
@* 
                <NavLink href="Account/Register"
                         class="sidebar-link">

                    <i class="bi bi-person-plus"></i>

                    <span>Registrarse</span>

                </NavLink> *@

            </NotAuthorized>

        </AuthorizeView>

    </div>

</div>

@code {
    private string? currentUrl;

    protected override void OnInitialized()
    {
        currentUrl = NavigationManager.ToBaseRelativePath(NavigationManager.Uri);
        NavigationManager.LocationChanged += OnLocationChanged;
    }

    private void OnLocationChanged(object? sender, LocationChangedEventArgs e)
    {
        currentUrl = NavigationManager.ToBaseRelativePath(e.Location);
        StateHasChanged();
    }

    public void Dispose()
    {
        NavigationManager.LocationChanged -= OnLocationChanged;
    }
}

